{
  "name": "my-crystal-bracelet",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "node server.js",
    "client": "cd client && npm start",
    "dev": "concurrently \"npm run start\" \"npm run client\"",
    "vercel-build": "cd client && npm ci && npm run build"
  },
  "dependencies": {
    "concurrently": "^8.0.1",
    "express": "^4.18.2"
  }
}
